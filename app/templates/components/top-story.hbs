{{#link-to 'articl' topStory.id}}
<div class="stories">
  <div class="row">
    <div class="col-md-6">
      <img src={{topStory.picture}}>
    </div>
    <div class="col-md-6">
      <h3>{{topStory.title}}</h3>
      <p>{{topStory.description}}</p>
      <p>By: {{topStory.author}}</p>
    </div>
  </div>
</div>
{{/link-to}}
